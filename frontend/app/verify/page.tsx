'use client'; import AuthShell from '../../components/AuthShell';
export default function Verify(){return <AuthShell title="Verify Your Account" subtitle="Complete secure verification to activate your workspace."><div className="auth-card compact"><h2>Enter verification code</h2><p>Enter the 6-digit code sent to your registered email.</p><form onSubmit={e=>{e.preventDefault();location.href='/dashboard'}}><div className="otp">{[0,1,2,3,4,5].map(i=><input key={i} inputMode="numeric" maxLength={1} aria-label={`Digit ${i+1}`}/>)}</div><button className="primary">Verify & Continue</button></form><button className="linkbtn">Resend code</button></div></AuthShell>}
