# Example values for a Redis HA/Sentinel Helm chart. Pin a chart/version in your environment.
architecture: replication
replica:
  replicaCount: 3
sentinel:
  enabled: true
master:
  persistence:
    enabled: true
    size: 10Gi
replica:
  replicaCount: 3
  persistence:
    enabled: true
    size: 10Gi
metrics:
  enabled: true
